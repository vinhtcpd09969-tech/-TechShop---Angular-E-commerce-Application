import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.html',
  standalone: false
})
export class App {
  // 🎉 Tuyệt vời! File App giờ đây vô cùng sạch sẽ.
  // Nhiệm vụ của nó lúc này chỉ là hiển thị Navbar và Router Outlet.
  // Mọi dữ liệu và logic đã được chia nhỏ về đúng nơi quy định (Services và các Pages).
}